#include <cstdio>
#include <cstring>
#include <iostream>
#include <algorithm>

using namespace std;

const int N = 60;
char mat[N][N];
int n, m;

bool dfs(int x, int y)
{

}

int main()
{
	while(scanf("%d%d", &n, &m)!=EOF)
	{
		for(int i = 0; i < n; i++)
		  	scanf("%s", mat[i]);
	}

	return 0;
}
